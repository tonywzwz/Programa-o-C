#include <stdio.h>
 #include <stdlib.h>
 #include <ctype.h>
 #include <locale.h>
 
 int main () {  
 	int numeros [5];
 	int i;
 	
printf("Digite os elementos dos vetor: \n");
	for (i = 0; i < 5; i++) {
		printf("Elemento %d:", i + 1);
		scanf ("%d",&numeros[i]);
	
	for (i - 0; i < 5; i++) {
		
		printf("Elemento %d: %d\n", i + 5, vetor[1])
	}
		
	}
	
	return 0;
}
